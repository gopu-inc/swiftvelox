// Hello World amélioré
fn main() {
    swget("🌟 BONJOUR SWIFTVELOX! 🌟")
    
    let nombre = 42
    let texte = "La réponse universelle est: "
    
    swget(texte + nombre as string)
    
    // Petit calcul
    let a = 7
    let b = 6
    swget("7 * 6 = " + (a * b) as string)
    
    swget("✨ Langage compilé natif! ✨")
}
