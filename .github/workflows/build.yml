name: Notify Discord

on:
  push:
    branches:
      - main
  release:
    types: [published]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord notification on push
        if: github.event_name == 'push'
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d "{\"content\":\"📂 Un commit vient d’être poussé sur main par ${{ github.actor }} : ${{ github.event.head_commit.message }}\"}" \
          https://discord.com/api/webhooks/1465454947968614526/-7OTF0_zc9q_EQ347Y5PhRRAOzyENISnvByDE_N1MzcUH7ILx8Ym19LJfh88pDq53iLT

      - name: Send Discord notification on release
        if: github.event_name == 'release'
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d "{\"content\":\"🚀 Nouvelle release publiée: ${{ github.event.release.name }} - ${{ github.event.release.html_url }}\"}" \
          https://discord.com/api/webhooks/1465454947968614526/-7OTF0_zc9q_EQ347Y5PhRRAOzyENISnvByDE_N1MzcUH7ILx8Ym19LJfh88pDq53iLT
